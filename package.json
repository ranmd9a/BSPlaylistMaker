{
  "name": "bs-playlistmaker",
  "version": "0.0.4",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "rm -rf dist && mkdir dist && cp -p *.js dist && cp -p manifest.json dist && cp -p icon.png dist"
  },
  "author": "ranmd9a",
  "license": "MIT"
}
